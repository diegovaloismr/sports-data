SQLAlchemy>=2.0
PyYAML>=6.0
python-dotenv>=1.0
gspread>=6.0
google-auth>=2.30

# Camada 2 - dashboard
streamlit>=1.38
plotly>=5.24
pandas>=2.2
# pyarrow>=18 exige macOS 12+; travado em <18 para continuar instalável
# em macOS 11 (Big Sur) sem precisar compilar do zero.
pyarrow<18

# Testes
pytest>=8.0
