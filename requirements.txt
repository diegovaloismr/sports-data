SQLAlchemy>=2.0
PyYAML>=6.0
python-dotenv>=1.0
gspread>=6.0
google-auth>=2.30

# Camada 2 - dashboard
streamlit>=1.38
plotly>=5.24
pandas>=2.2
# pyarrow>=18 exige macOS 12+. Fixado numa versão exata (não só um teto)
# porque, resolvendo junto com o resto do requirements.txt, o pip às
# vezes escolhe uma versão de pyarrow<18 sem wheel pronto para macOS,
# tentando compilar do zero e falhando sem toolchain de build instalado.
pyarrow==17.0.0
# numpy 2.x muito recente (ex: 2.4) quebra a ABI binária do pyarrow 17
# (compilado contra uma versão mais antiga da API do NumPy) - trava com
# "segmentation fault"/"_ARRAY_API not found" em vez de um erro Python
# normal. numpy<2 é a combinação mais testada com pyarrow/pandas.
numpy<2

# Testes
pytest>=8.0
